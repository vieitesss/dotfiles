---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs as we go.
disable-model-invocation: true
---

Run a `/grilling` session, using:
- If in TDD (there is a `CONTEXT.md` file) -> the `/context-modeling` skill.
- If in SDD (there is a `specs` directory) -> the `/development-worflow` or `/sdd` skill, whichever is available.
